{
  "name": "deployed-to-do-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server &&(cd server && npm install) || (cd client && install)",
    "server": "npde ./server/server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
